# 개발자 요청 검토 — add_to_cart 이벤트

## 이미 확인된 내용
- 이벤트 이름: `add_to_cart`
- 발생 위치: 상품 상세 페이지의 「장바구니에 담기」 버튼
- 발생 시점: 버튼을 누른 순간
- 파라미터: `currency`(KRW), `value`(가격×수량), `items`(무쇠 주전자 / p1 / 가격 48000 / 수량 1)
- 목적: 담기는 많은데 결제로 넘어가지 않는 상품을 파악해 장바구니 화면 개선 여부를 판단

## 질문과 답변

| 질문 | 답변 |
| --- | --- |
| 이미 장바구니에 담겨 있는 상품을 다시 담았을 때, `quantity`/`value`를 "이번에 추가된 수량" 기준으로 보낼지, "재담기 후 장바구니 내 총 수량" 기준으로 보낼지? | 재담기 후 총수량이 아니라 이번 행동에서 새로 추가한 수량 기준. 예를 들어 기존에 1개가 담겨 있어도 이번에 1개를 추가했다면 quantity는 1, value는 이번에 추가한 1개의 가격 × 수량 |
| 이벤트를 GTM의 `dataLayer.push`로 보낼지, `gtag()` 직접 호출로 보낼지? | 구현 방법은 지정하지 않음. 개발자가 결정 |
| `add_to_cart` 외에 `begin_checkout`/`purchase` 같은 후속 이벤트도 이번에 같이 구현해야 하는지? | 이번 요청 범위는 `add_to_cart` 하나. `begin_checkout`, `purchase`는 측정 계획서에 별도 이벤트로 정의되어 있고 각각 별도 구현 요청으로 전달 예정 |

## 확정된 요청 문장

상품 상세에서 「장바구니에 담기」 단추를 누른 순간, GA4로 `add_to_cart` 이벤트를 보내 주세요.

같이 보낼 값은 아래와 같습니다.
- currency : KRW
- value : 이번에 새로 담은 상품의 가격 × 수량 (예: 무쇠 주전자 48000 × 1 = 48000). 이미 장바구니에 같은 상품이 담겨 있어도 총수량이 아니라 이번에 새로 추가한 수량 기준으로 계산합니다.
- items : 이번에 새로 담은 상품 — 무쇠 주전자(p1), 가격 48000, 수량 1 (재담기 시에도 누적 수량이 아닌 이번 추가분만 반영)

전송 방식(dataLayer.push / gtag() 등)은 개발자가 정합니다.

이번 요청 범위는 `add_to_cart` 이벤트 하나이며, `begin_checkout` · `purchase`는 별도 요청으로 전달합니다.

담기는 많은데 결제로 안 넘어가는 상품을 보고 장바구니 화면을 손볼지 정하려는 목적입니다.

## 비고
- `product.html`/`assets/app.js`를 확인한 결과, 상품 상세 페이지에는 수량 선택 UI가 없어 버튼 클릭 시 항상 1개씩 담기는 구조입니다. 요청에 있는 수량 1, 가격 48000 예시와 일치합니다.
- 사이트 전체에서 `add_to_cart`나 유사한 이커머스 이벤트를 보내는 기존 코드는 없습니다(신규 구현).
